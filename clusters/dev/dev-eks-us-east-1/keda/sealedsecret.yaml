# Placeholder: replace with the output of kubeseal (steps in docs/keda/README.md).
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: keda-irsa
  namespace: flux-system
spec:
  encryptedData:
    values.yaml: REPLACE_ME
  template:
    metadata:
      name: keda-irsa
      namespace: flux-system
